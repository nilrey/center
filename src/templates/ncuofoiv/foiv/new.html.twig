{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Foiv creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('foiv') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
